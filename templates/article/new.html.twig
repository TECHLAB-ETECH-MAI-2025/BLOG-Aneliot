{% extends 'base.html.twig' %}

{% block title %}New Article{% endblock %}

{% block body %}
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-body">
          <h1 class="card-title mb-4 text-center">Create new Article</h1>

          {{ include('article/_form.html.twig') }}

          <div class="text-center mt-3">
            <a href="{{ path('app_article_index') }}" class="btn btn-outline-secondary btn-sm">Back to list</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

{% endblock %}
